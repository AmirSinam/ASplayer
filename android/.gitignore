# The Gradle wrapper is committed on purpose: without gradlew and its jar,
# a fresh clone (and every CI build) has no way to run Gradle at all.
/.gradle
/captures/
/local.properties
GeneratedPluginRegistrant.java
.cxx/

# Remember to never publicly share your keystore.
# See https://flutter.dev/to/reference-keystore
key.properties
**/*.keystore
**/*.jks
